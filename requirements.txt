# This repo pins one engine version; the workflows install the same pin
# (resolving the owner as ${{ github.repository_owner }}). For local
# development against a sibling checkout: pip install -e ../wss
#
# v0.6.57 is the FLOOR: it is the first version with `project_drop`, which this
# repo declares to halve its capture size. Any earlier engine rejects the
# registry at `wss validate` with "unknown key 'project_drop'".
# NOTE: 0.6.57 is not pushed yet -- it must be tagged before CI can install it.
wss[object] @ git+https://github.com/q3dresearch/wss-engine.git@v0.6.57
